<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Open Lecture Time</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
        }
        header {
            background-color: white;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            height: 40px;
        }
        .icons {
            display: flex;
            gap: 20px;
        }
        .icon {
            font-size: 24px;
            color: #333;
            text-decoration: none;
        }
        main {
            padding: 20px;
        }
        h1 {
            margin-bottom: 20px;
        }
        .filters {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }
        .filter {
            background-color: #a3e4f3;
            border: none;
            padding: 10px 20px;
            border-radius: 20px;
            cursor: pointer;
        }
        .lecture {
            background-color: white;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
        }
        .lecture-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }
        .lecture-time {
            background-color: #a3e4f3;
            padding: 5px 10px;
            border-radius: 15px;
        }
        .lecture-title {
            font-size: 18px;
            font-weight: bold;
        }
        .lecture-instructor {
            color: #666;
        }
        .tags {
            display: flex;
            gap: 10px;
            margin-bottom: 10px;
        }
        .tag {
            background-color: #e0e0e0;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 14px;
        }
        .lecture-details {
            display: flex;
            justify-content: space-between;
            color: #666;
            font-size: 14px;
        }
        .lecture-description {
            margin-top: 10px;
            font-size: 14px;
            color: #333;
        }
    </style>
</head>
<body>
    <header>
        <img src="/open_lecture_time_logo.png" alt="Open Lecture Time" class="logo">
        <div class="icons">
            <a href="#" class="icon">🔔</a>
            <a href="#" class="icon">👤</a>
        </div>
    </header>
    <main>
        <h1>비교과 프로그램</h1>
        비교과 프로그램
<div class="filters">
  <select id="daySelect">
    <option value="">월요일 ▼</option>
    <option value="월요일">월요일</option>
    <option value="화요일">화요일</option>
    <option value="수요일">수요일</option>
    <option value="목요일">목요일</option>
    <option value="금요일">금요일</option>
    <option value="토요일">토요일</option>
  </select>

  <select id="interestSelect" onchange="filterLectures()">
    <option value="">관심분야 ▼</option>
    <% interests.forEach(interest => { %>
      <option value="<%= interest %>"><%= interest %></option>
    <% }); %>
  </select>
</div>

<% lectures.forEach(lecture => { %>
  <div class="lecture-item" data-day="<%= lecture.day %>">
    <h3><%= lecture.title %></h3>
    <p>시간: <%= lecture.time %></p>
    <p>강사: <%= lecture.instructor %></p>
    <p>태그: <%= lecture.tags.join(', ') %></p>
    <p>기간: <%= lecture.duration %></p>
    <p>장소: <%= lecture.location %></p>
    <p>설명: <%= lecture.description %></p>
  </div>
<% }); %>

<script>
function filterLectures() {
  const interest = document.getElementById('interestSelect').value;
  window.location.href = `/?interest=${interest}`;
}

document.getElementById('daySelect').addEventListener('change', function() {
  const selectedDay = this.value;
  const lectures = document.getElementsByClassName('lecture-item');
  
  for (let lecture of lectures) {
    if (selectedDay === '' || lecture.dataset.day === selectedDay) {
      lecture.style.display = 'block';
    } else {
      lecture.style.display = 'none';
    }
  }
});
</script>
    </main>
</body>
</html>