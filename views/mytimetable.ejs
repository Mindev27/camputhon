<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Open Lecture Time - 시간표</title>
    <style>
        .timetable td {
            border: 1px solid #ccc;
            padding: 5px;
        }
        .highlight {
            background-color: lightblue;
        }
    </style>
    <link rel="stylesheet" href="3page.css">
</head>
<body>
    <header>
        <img src="/open_lecture_time_logo.png" alt="Open Lecture Time" class="logo">
    </header>
    <main>
        <div class="timetable-container">
            <div class="header-container">
                <h2><%= data.username %>의 시간표</h2>
                <a href="/other-page" class="navigation-button">시간표 수정하기</a>
            </div>
            <table class="timetable">
                <thead>
                    <tr>
                        <th></th>
                        <th>MON</th>
                        <th>TUE</th>
                        <th>WED</th>
                        <th>THU</th>
                        <th>FRI</th>
                        <th>SAT</th>
                    </tr>
                </thead>
                <tbody>
                    <% for (let hour = 9; hour <= 22; hour++) { %>
                        <tr>
                            <th><%= hour %></th>
                            <% ['mon', 'tue', 'wed', 'thu', 'fri', 'sat'].forEach(day => { %>
                                <% 
                                    const cellId = `${day}-${hour}`;
                                    const isSelected = data.selectedCells.includes(cellId);
                                %>
                                <td id="<%= cellId %>" class="<%= isSelected ? 'selected' : '' %>"></td>
                            <% }); %>
                        </tr>
                        <tr>
                            <th>ㅤ</th>
                            <% ['mon', 'tue', 'wed', 'thu', 'fri', 'sat'].forEach(day => { %>
                                <% 
                                    const cellId = `${day}-${hour}-30`;
                                    const isSelected = data.selectedCells.includes(cellId);
                                %>
                                <td id="<%= cellId %>" class="<%= isSelected ? 'selected' : '' %>"></td>
                            <% }); %>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </main>
</body>
</html>
